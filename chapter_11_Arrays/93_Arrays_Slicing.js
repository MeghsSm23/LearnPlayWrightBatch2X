// Slicing & Combining

//. // slice(start, end) — returns new array, does NOT mutate actual -> ( start, end-1) . index = 0

let arr = [1, 2, 3, 4, 5];
console.log(arr.slice(1, 4)); //rule - start - end -1


console.log(arr.slice(2));

console.log(arr.slice(-2));

console.log(arr.slice(0));